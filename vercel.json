{
  "rewrites": [{ "source": "/sitemap.xml", "destination": "/api/generateSitemap" }],
  "functions": {
    "api/generateSitemap.ts": {
      "memory": 1024
    }
  },
  "builds": [
    {
      "src": "api/hydration.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/hydration.ts"
    }
  ]
}
